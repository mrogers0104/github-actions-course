
name: 04 - Using Actions

on:
    push:

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Checkout Code
              run: ls
            - name: Printing Folders
              run: ls